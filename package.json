{
  "name": "old-guitar-bye",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "db:migrate": "node scripts/migrate.js",
    "test": "node --test"
  }
}
